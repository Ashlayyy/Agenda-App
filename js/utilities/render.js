class Renderer{
    render(placeToRender, thingToRender) {
        document.querySelector(placeToRender).appendChild(thingToRender);
    }
    //Maakt een functie aan in de classe die ervoor zorgt dat je elementen aan elkaar kan vastmaken
}